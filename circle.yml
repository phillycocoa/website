checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive

machine:
  pre:
    - git config --global user.name "CircleCI"
    - git config --global user.email "circleci@circleci.com"

dependencies:
  pre:
    - wget -O ~/tmp/hugo_0.17_linux_amd64.tar.gz https://github.com/spf13/hugo/releases/download/v0.17/hugo_0.17_linux_amd64.tar.gz
    - cd ~/tmp && tar xzf hugo_0.17_linux_amd64.tar.gz
    - mv ~/tmp/hugo_0.17_linux_amd64/hugo_0.17_linux_amd64 ~/bin/hugo

test:
  override:
    - hugo -v

deployment:
  master:
    branch: master
    commands:
      - git clone git@github.com:phillycocoa/phillycocoa.github.io.git public
      - ./deploy.sh
